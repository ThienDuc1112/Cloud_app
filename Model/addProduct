var pg_conn = require('./database');

async function addProduct(id, name, quantity, price) {
    const query = {
        text: `insert into product values ($1,$2,$3,$4)`,
        values: [id, name, quantity, price]
    }
    var query_data = pg_conn.query(query);
    return query_data;
}
module.exports = addProduct;